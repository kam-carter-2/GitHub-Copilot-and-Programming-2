<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Simple Social Feed</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f6f7;
      margin: 0;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    .feed {
      max-width: 600px;
      margin: 0 auto;
    }

    .post {
      background: white;
      border-radius: 10px;
      padding: 15px;
      margin-bottom: 20px;
      box-shadow: 0px 2px 5px rgba(0,0,0,0.1);
    }

    .username {
      font-weight: bold;
      color: #1877f2;
    }

    .content {
      margin: 10px 0;
    }

    .likes {
      margin: 10px 0;
      color: #555;
    }

    button {
      background-color: #1877f2;
      color: white;
      border: none;
      border-radius: 5px;
      padding: 5px 10px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0f5fc4;
    }

    .comments {
      margin-top: 15px;
      border-top: 1px solid #ddd;
      padding-top: 10px;
    }

    .comment {
      margin: 5px 0;
      font-size: 14px;
    }

    input[type="text"] {
      width: 80%;
      padding: 5px;
      margin-right: 5px;
    }
  </style>
</head>
<body>
  <h1>Simple Social Feed</h1>
  <div class="feed">
    <!-- Example Post -->
    <div class="post">
      <div class="username">@alice</div>
      <div class="content">Loving this new healthy smoothie recipe! 🥤</div>
      <div class="likes">Likes: <span class="like-count">0</span></div>
      <button class="like-btn">Like</button>

      <div class="comments">
        <div><strong>Comments:</strong></div>
        <div class="comment-list"></div>
        <input type="text" class="comment-input" placeholder="Write a comment...">
        <button class="comment-btn">Post</button>
      </div>
    </div>

    <!-- Another Post -->
    <div class="post">
      <div class="username">@bob</div>
      <div class="content">Just finished a 5k run 🏃‍♂️ Feeling great!</div>
      <div class="likes">Likes: <span class="like-count">0</span></div>
      <button class="like-btn">Like</button>

      <div class="comments">
        <div><strong>Comments:</strong></div>
        <div class="comment-list"></div>
        <input type="text" class="comment-input" placeholder="Write a comment...">
        <button class="comment-btn">Post</button>
      </div>
    </div>
  </div>

  <script>
    // Handle Likes
    document.querySelectorAll(".like-btn").forEach((btn, index) => {
      btn.addEventListener("click", () => {
        const likeCount = btn.previousElementSibling.querySelector(".like-count");
        likeCount.textContent = parseInt(likeCount.textContent) + 1;
      });
    });

    // Handle Comments
    document.querySelectorAll(".comment-btn").forEach((btn) => {
      btn.addEventListener("click", () => {
        const input = btn.previousElementSibling;
        const commentList = btn.parentElement.querySelector(".comment-list");

        if (input.value.trim() !== "") {
          const newComment = document.createElement("div");
          newComment.classList.add("comment");
          newComment.textContent = input.value;
          commentList.appendChild(newComment);
          input.value = "";
        }
      });
    });
  </script>
</body>
</html>

